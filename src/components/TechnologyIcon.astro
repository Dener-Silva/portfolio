---
const { name } = Astro.props;
const src = `${import.meta.env.BASE_URL}${name
    ?.replace(/\W/g, "-")
    .toLowerCase()}.svg`;
---

<img src={src} style="filter: brightness(0);" height="23px" />
